@model IEnumerable<TechInvMgmt.Models.SerialNumber>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PartNum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subinv)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PartNum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subinv)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SerialNum">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SerialNum">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SerialNum">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
